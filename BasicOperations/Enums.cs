using System;

namespace DSA
{
    namespace Enums
    {
        enum DSA_Options
        {
            Linkedlist, Arrays, Strings, Stacks, Queues, Trees, Graphs, EXIT
        }
        enum LinkedList_Options
        {
            Display, Insert, Search, Update, Reverse, Sort, Merge, EXIT
        }
    }
}


